import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ReservedTasks = () => {
  return (
    <View>
      <Text>ReservedTasks</Text>
    </View>
  );
};

export default ReservedTasks;

const styles = StyleSheet.create({});
